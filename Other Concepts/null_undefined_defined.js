//undefined
let a;
console.log(a);

//undefined means value has not been assigned, by default also undefined
let arr = [];
console.log(arr[0])

function sum(x,y)
{
    let sumi = x+y;
}

pp = sum(2,3)
console.log(pp) //undefined as there is not return value


//NULL is just an explicit value that we assign to vraibles or objects or anything
let myname = null
console.log(myname)